癤?define _CRT_NO_SECURE_WARNINGS
#include <stdio.h>

int main0() {
	// ??대Ц : 議곌굔臾? 諛蹂듬Ц, 湲고??대Ц
	// ??대Ц : ?? 肄?? ??? ??濡, ?쇱そ?? ?ㅻⅨ履쎌쇰? 吏????吏留 洹??由? 諛轅 ? ?? 臾몃?
	// if, switch, for, while, break, continue, goto, dowhile

	// 議곌굔臾?if~else if~else
	// if(){} : ()?? 議곌굔??, {}?? 議곌굔???留??留 ?ㅽ? 肄?瑜???쇰㈃ ??	int target = 5;
	if (target == 5) {
		printf("5? 5? 媛??n");
	}
	else if (target != 5) {
		printf("5? 5? 媛吏 ???n");
	}
	else {
		printf("紐⑤Ⅴ寃??n");
	}

	// 議곌굔臾?switch~case~break
	// switch(){} : () ?댁⑷낵 case? ?댁⑹?媛? 遺遺遺??肄? ?ㅽ
	switch (target) {
	case 1:
		printf("??? 1怨?媛??);
		break;
	case 5:
		printf("??? 5? 媛??);
		break;
	default:
		printf("??怨?媛? 媛?????);
	}

	// 諛蹂듬Ц while
	// while? ??洹?i媛 ???? 以蹂듭??瑜?愿由?	int i = 0;
	while (i < 3) {
		printf("\n諛蹂듯? %d踰\n", i + 1);		// i蹂대?1 ? ???レ 異??		i++;		// 蹂? i? ?ㅼ댁? 媛? 1利媛???(i = i + 1;)
	}

	for (int i = 0; i < 3; i++) {
		printf("\n諛蹂듯? %d?\n", i + 1);		// 諛蹂듯怨??? 肄?留 ?⑥
	}

	// 湲고??대Ц continue, break
	// break : 諛蹂듬Ц? 醫猷 (switch醫猷)
	// continue : 諛蹂듬Ц 泥? ()濡 ??媛 (1???痍⑥)
	for (int i = 0; i < 3; i++) {
		if (i == 2) {
			break;			// i媛 2?쇰 諛蹂듬Ц??醫猷
		}
	}

	for (int i = 0; i < 3; i++) {
		if (i == 1) {
			continue;		// continue蹂대?諛? ?? 肄?? 臾댁?怨 ?濡 ?щ쇨?
		}
	}

	return 0;			// ??щ?}